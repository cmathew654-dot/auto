---
phase: 01-demo-destination-panel-sample
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-demo.mjs
  - portfolio-public.test.cjs
autonomous: true
requirements: [TRST-02]

must_haves:
  truths:
    - "The deployed demo's build badge shows a real short git SHA and a build date, not 'Build dev'"
  artifacts:
    - path: "scripts/build-demo.mjs"
      provides: "Stamps window.__BUILD_INFO__ = { sha, builtAt } into the generated demo-dist/index.html from git metadata"
      contains: "__BUILD_INFO__"
  key_links:
    - from: "scripts/build-demo.mjs"
      to: "demo-dist/index.html"
      via: "inline <script> defining window.__BUILD_INFO__ before the module script that calls renderLocalMvpShell"
      pattern: "__BUILD_INFO__"
    - from: "demo-dist/index.html"
      to: "main.ts renderLocalMvpShell build pill"
      via: "window.__BUILD_INFO__ read at line 126 of main.ts"
      pattern: "buildInfo\\?\\.sha"
---

<objective>
Make the demo build stamp real git metadata into the generated page so the build badge stops falling back to "Build dev".

Purpose: `main.ts` (line 126) already reads `window.__BUILD_INFO__` and renders `Build {sha} · {date}`, but `scripts/build-demo.mjs` never defines it — so every deployed build, including the public GitHub Pages demo, silently renders the "Build dev" fallback. The provenance claim on the page is currently false.
Output: A build script that resolves the short SHA and build timestamp and injects them into `demo-dist/index.html`.
</objective>

<execution_context>
@C:/Users/Cyril/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Cyril/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/build-demo.mjs
@portfolio-public.test.cjs
@.github/workflows/pages.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stamp git SHA and build date into demo-dist/index.html</name>
  <files>scripts/build-demo.mjs</files>
  <action>
In `scripts/build-demo.mjs` (which already imports `spawnSync` from `node:child_process`), add a small resolver above the `writeFileSync(join(outDir, 'index.html'), ...)` call:

- `sha`: run `git rev-parse --short HEAD` via `spawnSync('git', [...], { encoding: 'utf8' })` and trim stdout. If the command fails (status !== 0 or empty stdout), fall back to `process.env.GITHUB_SHA?.slice(0, 7)`, then to `null`.
- `builtAt`: run `git log -1 --format=%cI` and trim; if that fails, use `new Date().toISOString()`. `main.ts` renders `builtAt.slice(0, 10)`, so any ISO-8601 string works.

Inject into the generated HTML, immediately before the existing `<script type="module">` block so it runs first:

```html
<script>window.__BUILD_INFO__ = {"sha":"...","builtAt":"..."};</script>
```

Serialize with `JSON.stringify({ sha, builtAt })` rather than string concatenation. If `sha` resolves to `null`, omit the inline script entirely so `main.ts` keeps its honest "Build dev" fallback — never stamp a placeholder or fake SHA.

Do NOT touch `main.ts`; the reader side (line 126-132) already handles this shape. Do not change the `how-it-started` copy step at the end of the script.

The GitHub Pages workflow (`.github/workflows/pages.yml`) uses `actions/checkout@v4` before `npm run build:demo`, so the `.git` directory is present and `git rev-parse` resolves in CI; the `GITHUB_SHA` fallback covers a shallow/detached edge case.
  </action>
  <verify>Run `npm run build:demo`, then confirm `demo-dist/index.html` contains `window.__BUILD_INFO__` with a 7-character SHA matching `git rev-parse --short HEAD` and an ISO date in `builtAt`.</verify>
  <done>`demo-dist/index.html` carries real build metadata, and the build still succeeds when git metadata is unavailable (no inline script emitted, no crash).</done>
</task>

<task type="auto">
  <name>Task 2: Assert the build stamp in portfolio-public.test.cjs</name>
  <files>portfolio-public.test.cjs</files>
  <action>
`portfolio-public.test.cjs` already reads `scripts/build-demo.mjs` in its second test. Add a new `test(...)` case in the same file (no `package.json` change needed — this file is already in the `npm test` list) asserting the build script:
- runs `git rev-parse --short HEAD` (match `/rev-parse\s+--short\s+HEAD/`),
- writes `window.__BUILD_INFO__` into the generated index.html (match `/__BUILD_INFO__/`),
- has a fallback path referencing `GITHUB_SHA`.

Also assert the negative: the script must not hardcode a literal seven-hex-character SHA string as the stamped value (guard against someone pinning a fake build id). A regex such as `/"sha"\s*:\s*['"][0-9a-f]{7}['"]/` must not match.

Keep the existing tests in that file untouched.
  </action>
  <verify>`npm test` passes with the new assertions.</verify>
  <done>A test fails if the build stamp is removed, hardcoded, or loses its CI fallback.</done>
</task>

</tasks>

<verification>
- `npm run build:demo` produces `demo-dist/index.html` containing `window.__BUILD_INFO__` with the current short SHA.
- `npm test` passes.
- Serving `demo-dist` (`npm run start:demo`) shows a build pill reading `Build {sha} · {YYYY-MM-DD}` instead of `Build dev`.
</verification>

<success_criteria>
The build badge on a freshly built demo shows the real short git SHA and build date; "Build dev" appears only when git metadata genuinely cannot be resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-destination-panel-sample/01-02-SUMMARY.md`
</output>
