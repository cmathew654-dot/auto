---
phase: 01-demo-destination-panel-sample
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - main.ts
  - review-export-surface.ts
  - demo-flow.test.cjs
  - package.json
autonomous: true
requirements: [DEMO-01, DEMO-03, SMPL-04, TRST-01]

must_haves:
  truths:
    - "A visitor clicks one button and all four pipeline stages run with no file upload, credentials, or setup"
    - "At stage 4 the simulated destination panel appears in-page with a demo Fill button and no bookmarklet install step"
    - "Only eligible rows reach the destination panel; blocked rows stay behind and are visibly reported as withheld"
    - "The safety model — browser processing, no project server, manual save, synthetic demo data — is legible from the page itself"
  artifacts:
    - path: "review-export-surface.ts"
      provides: "onFillPacketReady option handing the live EmoneyFillPacket (eligible rows only) to the caller whenever it is rebuilt"
      contains: "onFillPacketReady"
    - path: "main.ts"
      provides: "Single Run the full demo button driving load -> review -> packet -> destination panel fill, plus the synthetic-demo-data safety line"
      contains: "renderDemoDestinationPanel"
    - path: "demo-flow.test.cjs"
      provides: "Asserts the packet built from the demo sample carries only eligible rows and that blockedCount matches the withheld rows"
      min_lines: 25
  key_links:
    - from: "review-export-surface.ts"
      to: "main.ts"
      via: "opts.onFillPacketReady(activeFillPacket) fired from renderTransferPacket"
      pattern: "onFillPacketReady"
    - from: "main.ts demo Fill button"
      to: "paste-conductor runDemoFill"
      via: "direct call with the live packet and the panel root, no bookmarklet"
      pattern: "runDemoFill\\("
    - from: "main.ts"
      to: "demo-destination-panel renderDemoDestinationPanel"
      via: "panel rendered into a stage-4 container revealed by the one-click run"
      pattern: "renderDemoDestinationPanel\\("
---

<objective>
Wire the pieces into one visitor-facing flow: one button runs all four stages, the destination panel appears at stage 4, a demo Fill button drives the paste conductor directly, blocked rows are visibly withheld, and the safety model reads off the page.

Purpose: Plans 01-01 and 01-03 produce a mixed-verdict sample and a fill target, but nothing connects them. Today a visitor must load the sample, click Copy Fill Packet, install a bookmarklet, and have an eMoney session — which is the entire reason the public demo dead-ends.
Output: A one-click demo path in `main.ts`, a packet handoff in `review-export-surface.ts`, and a test pinning eligible-rows-only.
</objective>

<execution_context>
@C:/Users/Cyril/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Cyril/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@main.ts
@review-export-surface.ts
@paste-conductor.ts
@.planning/phases/01-demo-destination-panel-sample/01-01-SUMMARY.md
@.planning/phases/01-demo-destination-panel-sample/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand the live fill packet out of the review surface</name>
  <files>review-export-surface.ts</files>
  <action>
Add `onFillPacketReady?: (packet: EmoneyFillPacket | null) => void;` to `ReviewExportSurfaceOptions` (interface at line 50).

Fire it at the end of `renderTransferPacket()` with the current `activeFillPacket` (pass `null` when there is no packet or `rowCount === 0`). `renderTransferPacket` already re-runs when the manual-review override checkbox toggles, so the caller automatically receives a packet whose row set reflects the current override state — that is what makes the override control visibly change what reaches the destination panel.

Do not change how `activeFillPacket` is built: `buildEmoneyFillPacket` already receives only eligible holdings via `toAssistantPayloadForAccount` and carries `blockedCount` for the withheld rows (SMPL-04 depends on that existing behavior, not on new filtering).

Do not remove or alter the bookmarklet install card, the Copy Fill Packet button, or the clipboard path — the operator-facing eMoney flow stays intact; the demo path is additive.
  </action>
  <verify>`npm run typecheck` and `npm test` pass; `git diff review-export-surface.ts` is limited to the options interface and the `renderTransferPacket` callback.</verify>
  <done>Callers of `renderReviewExportSurface` receive the live eligible-rows-only fill packet, including after an override toggle.</done>
</task>

<task type="auto">
  <name>Task 2: One-click demo run + destination panel + demo Fill button</name>
  <files>main.ts</files>
  <action>
In `renderLocalMvpShell` (main.ts):

1. **Stage-4 container**: after `reviewRoot` (line 252-254), create `const destinationRoot = document.createElement('section')` with id `demo-destination-root`, hidden by default (`hidden = true`), appended to the shell.

2. **One-click run (DEMO-01)**: replace the existing `sampleButton` ("Load demo sample") handler with a `Run the full demo` button that, on a single click, with no file input and no credentials:
   - stage 1: `setWorkflowStep('load')`, `renderLedgerSkeleton`, `await stageLoadStatus(status, 'demo-sample.csv')`;
   - stage 2: call `runLocalMvp(reviewRoot, SAMPLE_CSV_INPUT, { ... })` passing a new `onFillPacketReady` handler that stores the packet in a `latestPacket` variable and enables the demo Fill button; `setWorkflowStep('review')`;
   - stage 3: `setWorkflowStep('packet')` once a non-null packet has arrived (the review surface builds and hands over the packet on its first render — no clipboard copy is required for the demo path);
   - stage 4: reveal `destinationRoot` (`hidden = false`), render the panel into it with `renderDemoDestinationPanel(destinationRoot)` (call `panel.reset()` if re-running), `setWorkflowStep('fill')`, and scroll it into view.
   Keep the existing "Choose CSV file", drag-and-drop, and Clear session paths working exactly as they do now (Clear session must also hide + reset the destination panel and null out `latestPacket`).

3. **Demo Fill button (DEMO-03)**: render a `Fill the simulated destination page` button inside `destinationRoot`'s header area (or immediately above the panel). Its click handler calls `await runDemoFill(panel.root, latestPacket, { onRow: ... })` from `./paste-conductor` — a direct call, no bookmarklet, no clipboard, no install step. While running, disable the button and show progress in the status line; on completion show `{n} eligible rows filled. {blockedCount} blocked rows were withheld.` using `latestPacket.rowCount` and `latestPacket.blockedCount`; on a thrown error show the error message in the error tone.

4. **Withheld rows are visible (SMPL-04)**: alongside the panel, render a short line naming what did NOT travel — e.g. `Withheld from the destination page: {blockedCount} row(s) that failed review.` Derive the count from `latestPacket.blockedCount`; do not recompute eligibility in main.ts.

5. **Safety model on the page (TRST-01)**: the header badges (line 120) and footer (line 258-261) already state BROWSER PROCESSING / NO PROJECT SERVER / Manual Save in eMoney. Add the missing fourth claim: a `SYNTHETIC DEMO DATA` badge in the same badges row, a `['Demo data', 'Synthetic only']` entry in the `ledger-checks` grid (line 236-241), and one footer sentence stating the demo sample is fabricated data and that saving in the real system is always a manual operator click. Keep the wording bounded — no "safe for real client data" style claims (see the forbidden-phrase list already enforced by `portfolio-public.test.cjs`).

Import `renderDemoDestinationPanel` from `./demo-destination-panel` and `runDemoFill` from `./paste-conductor`.
  </action>
  <verify>`npm run typecheck` passes; `npm run build:demo` succeeds; `npm run start:demo` serves a page where a single click on "Run the full demo" advances the stepper through all four steps and reveals the destination panel.</verify>
  <done>One button click runs load → review → packet → destination panel with no upload or setup; the demo Fill button fills the panel via `runDemoFill`; the withheld-row count and the four safety claims (browser processing, no project server, manual save, synthetic demo data) are all visible on the page.</done>
</task>

<task type="auto">
  <name>Task 3: Pin eligible-rows-only in demo-flow.test.cjs</name>
  <files>demo-flow.test.cjs, package.json</files>
  <action>
Create `demo-flow.test.cjs` (node:test, no DOM) that reproduces the packet the demo hands to the panel, using the compiled modules:

1. Parse `DEMO_SAMPLE_CSV` with `parseHoldingsCsvToIngestionFile`, take the single account, build the payload with `toAssistantPayloadForAccount(account)` (override OFF) and the packet with `buildEmoneyFillPacket(payload, { blockedCount })` exactly as `review-export-surface.ts` does — read that call site and mirror it rather than guessing the argument shape.
2. Assert (SMPL-04): every ticker in `packet.holdings` corresponds to a holding that `getHoldingEligibility` reports eligible; the hard-blocked row from the demo sample (the one with `MISSING_LOOKUP_KEY`) does NOT appear in `packet.holdings`; the manual-review-gated cash row does NOT appear with override OFF; `packet.blockedCount` equals the number of holdings withheld.
3. Assert the override actually changes the packet: rebuild with `allowManualOverride: true` and assert the manual-review row now appears while the `MISSING_LOOKUP_KEY` row still does not.
4. Assert (TRST-01) by reading `main.ts` text: it contains `SYNTHETIC DEMO DATA`, `NO PROJECT SERVER`, `BROWSER PROCESSING`, and a manual-save sentence.

Register `demo-flow.test.cjs` in the `test` script in `package.json`.
  </action>
  <verify>`npm test` passes with the new file.</verify>
  <done>A test fails if a blocked or review-gated row ever reaches the fill packet, if the override stops changing the packet contents, or if a safety claim is dropped from the page.</done>
</task>

</tasks>

<verification>
- `npm test`, `npm run typecheck`, `npm run build:demo` all pass.
- `npm run start:demo`: one click on "Run the full demo" walks the stepper to step 4 and reveals the panel; the demo Fill button populates the panel's table with only the eligible rows; the withheld count is displayed.
- The bookmarklet install card and CSV-upload path still work unchanged.
</verification>

<success_criteria>
A visitor with no file, no credentials, and no bookmarklet clicks one button, watches four stages run, sees the simulated destination panel appear, clicks Fill, and sees only the eligible rows land in the panel's table with the blocked rows reported as withheld — and can read the browser-processing / no-server / manual-save / synthetic-data safety model off the page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-destination-panel-sample/01-04-SUMMARY.md`
</output>
