---
phase: 01-demo-destination-panel-sample
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [DEMO-01, DEMO-02, DEMO-05, DEMO-06, DEMO-07, DEMO-08, SMPL-01, SMPL-04, TRST-01, TRST-02]

must_haves:
  truths:
    - "A human has watched the one-click demo run end to end in a real browser and confirmed each phase success criterion"
    - "The destination panel looks neutral to a human eye, not like eMoney"
    - "The build badge on the served build shows a real SHA and date"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the assembled demo in a real browser — the one thing the DOM-less node test suite cannot check.

Purpose: Every automated check in this phase is a source-text or pure-logic assertion (the test suite has no DOM). Whether the panel reads as neutral, whether the four stages are legible as they run, and whether the save confirmation reassures rather than misleads are all human judgments.
Output: Confirmation, or a list of defects to close.
</objective>

<execution_context>
@C:/Users/Cyril/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Cyril/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the assembled demo in a browser</name>
  <what-built>
The full Phase 1 demo path: a mixed-verdict synthetic sample (01-01), a real build stamp (01-02), the simulated destination panel and label-driven fill driver (01-03), and the one-click pipeline wiring plus safety copy (01-04).
  </what-built>
  <how-to-verify>
Run from the repo root:

```
cd C:/Users/Cyril/Projects/emoney-holdings-injector; npm test; npm run typecheck; npm run start:demo
```

Open the printed localhost URL and check, in order:

1. **One click, four stages (DEMO-01)** — click "Run the full demo". Confirm no file picker, no credential prompt, no setup step, and that the stepper visibly advances Load → Review → Prepare Fill Packet → Fill.
2. **Verdict mix (SMPL-01/SMPL-02)** — in the review table, confirm you can see at least one Eligible row, at least one row blocked for manual review (cash), and at least one row blocked for a missing ticker/CUSIP. Tick the "Override safety gate" checkbox and confirm the manual-review row flips to eligible while the missing-lookup-key row stays blocked.
3. **Destination panel (DEMO-02/DEMO-04/DEMO-06/DEMO-07)** — confirm the panel appears in-page at step 4 without navigating away; it shows the "Simulated destination page. This is not eMoney." banner; it has visible Ticker, CUSIP, Units, Cost Basis labels and a Save button; and nothing about it looks like eMoney (no logo, no eMoney colors, no eMoney page title).
4. **Fill (DEMO-03/DEMO-05/SMPL-04)** — click the demo Fill button. Confirm rows land one at a time into the panel's table, that ONLY the eligible rows appear, and that the withheld/blocked row count is stated on the page.
5. **Save (DEMO-08)** — click Save in the panel. Confirm the confirmation says the record is local to the simulated panel and never implies anything was saved into eMoney.
6. **Safety model + build badge (TRST-01/TRST-02)** — confirm the page states browser processing, no project server, manual save, and synthetic demo data; and that the build badge in the header shows a short SHA plus a date, not "Build dev".
  </how-to-verify>
  <resume-signal>Type "approved" or list the numbered checks that failed with what you saw.</resume-signal>
  <files>none (verification only — no files are modified by this plan)</files>
  <action>Build and serve the demo, then walk a human through the six checks in how-to-verify. Automate everything automatable first: run `npm test`, `npm run typecheck`, and `npm run build:demo` and report their results before handing over. Do not attempt to drive a browser on the user's behalf — the judgments here (neutral look, legible stages, reassuring save copy) are human-only.</action>
  <verify>All six numbered checks confirmed by the user in a real browser; `npm test` and `npm run typecheck` pass first.</verify>
  <done>User replies "approved", or a defect list exists and is recorded for gap closure.</done>
</task>

</tasks>

<verification>
All six numbered checks confirmed by a human in a real browser.
</verification>

<success_criteria>
The seven phase success criteria hold when a person actually uses the demo page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-destination-panel-sample/01-05-SUMMARY.md`
</output>
