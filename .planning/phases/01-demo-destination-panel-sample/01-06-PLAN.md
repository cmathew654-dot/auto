---
phase: 01-demo-destination-panel-sample
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - demo-sample.ts
  - demo-sample.test.cjs
  - demo-flow.test.cjs
autonomous: true
gap_closure: true
requirements: [SMPL-01, SMPL-02, SMPL-03, SMPL-04]

must_haves:
  truths:
    - "The demo sample carries two accounts and ~14 holding rows, so the run reads as custodial work rather than a toy"
    - "Account 1 parses to all-eligible rows with zero withheld, establishing what a clean account looks like"
    - "Account 2 parses to a mix: eligible rows, at least one manual-review-gated row, and at least one hard-blocked row"
    - "Every ticker, account number, owner, and issuer name in the sample is invented"
  artifacts:
    - path: "demo-sample.ts"
      provides: "Two-account synthetic DEMO_SAMPLE_CSV with a per-account verdict spread"
      contains: "900000002"
      min_lines: 25
    - path: "demo-sample.test.cjs"
      provides: "Per-account verdict-spread and synthetic-only assertions"
      contains: "accounts.length, 2"
  key_links:
    - from: "demo-sample.test.cjs"
      to: "parseHoldingsCsvToIngestionFile(DEMO_SAMPLE_CSV)"
      via: "parses the real sample and asserts the per-account spread"
      pattern: "parseHoldingsCsvToIngestionFile\\(DEMO_SAMPLE_CSV"
    - from: "demo-flow.test.cjs"
      to: "ingestion.accounts"
      via: "packet assertions run per account, not on accounts[0] only"
      pattern: "accounts\\["
---

<objective>
Replace the 4-row single-account demo sample with a two-account, ~14-row synthetic sample:
Account 1 all-clean, Account 2 carrying review-gated and hard-blocked rows.

Purpose: closes UAT GAP 2 ("um....it's two holdings?"). The sample must be big enough to
read as real custodial work, and the two-account shape is what the guided tour (plan 01-09)
narrates — Account 1 establishes "normal" so Account 2's stops read as judgment, not breakage.
Output: rewritten demo-sample.ts plus tests pinning the per-account spread and the 100%-synthetic guarantee.
</objective>

<execution_context>
@C:/Users/Cyril/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Cyril/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-destination-panel-sample/01-UAT.md

@demo-sample.ts
@demo-sample.test.cjs
@demo-flow.test.cjs
@holdings-csv-parser.ts
@review-export-surface.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DEMO_SAMPLE_CSV as a two-account, 14-row synthetic sample</name>
  <files>demo-sample.ts</files>
  <action>
Keep the existing exported name `DEMO_SAMPLE_CSV` and the existing header line verbatim:
`Account Description,Account Number,Owner,Last Updated,Symbol,Description,Quantity,Price,Cost Basis,Value,Asset Class`
(do not add or reorder columns — the parser's header detection and the account bucketing key
are already proven against this exact header).

Write 14 data rows across two accounts. All values invented; no real ticker, issuer, custodian,
or person may appear.

Account 1 — `Demo Household - Taxable Brokerage`, account number `900000001` (keep this number,
demo-sample.test.cjs asserts on it). Six rows, ALL eligible with override OFF:
  - Symbols DMOA, DMOB, DMOC, DMOD, DMOE, DMOF
  - Each row: non-empty Symbol, a distinct invented Description (e.g. `DEMO ALPHA HOLDINGS INC`),
    a non-zero Quantity, a non-zero Price, a Cost Basis, a Value, and a non-Cash Asset Class.
  - Keep Description and Quantity distinct per row so the parser's duplicate heuristic
    (`ticker_desc`, `ticker_units`, `desc_units` keys in holdings-csv-parser.ts) never fires.
  - No `$CASH$` symbol, no `Cash` asset class, no `$0.00` price — those trigger
    CASH_SPECIAL_HANDLING / ZERO_PRICE_NONZERO_VALUE_EXCEPTION, which would break the
    "Account 1 is clean" contract.

Account 2 — `Demo Household - Rollover IRA`, account number `900000002`. Eight rows:
  - Five clean rows (DMOG, DMOH, DMOI, DMOJ, DMOK) built the same way as Account 1's.
  - One cash row: Symbol `$CASH$`, Description `CASH`, blank Quantity, Price `$1.00`, blank
    Cost Basis, a Value, Asset Class `Cash` -> CASH_SPECIAL_HANDLING -> manual-review gated
    (override ON clears it).
  - One zero-price exception row: Symbol `DMOL`, an invented Description, a non-zero Quantity,
    Price `$0.00`, a NON-ZERO Value, non-Cash Asset Class -> ZERO_PRICE_NONZERO_VALUE_EXCEPTION
    -> manual-review gated (override ON clears it). This is the second thing the override gates.
  - One hard-blocked row: Symbol BLANK, Description `UNIDENTIFIED SECURITY POSITION`, a Quantity,
    a Price, a Value -> MISSING_LOOKUP_KEY -> blocked with override ON or OFF. The Description
    MUST stay non-empty: holdings-csv-parser.ts skips a row entirely when ticker, cusip, AND
    description are all blank, and the demo needs this row visible in the review table.

Update the file's top doc comment to describe the two-account shape and name which row triggers
which code. Keep the `(SMPL-03)` synthetic-data note.
  </action>
  <verify>`npx tsc -p tsconfig.test.json` compiles, and a quick node check on `.test-dist/demo-sample.js` parsed through `parseHoldingsCsvToIngestionFile` reports 2 accounts with 6 and 8 holdings.</verify>
  <done>DEMO_SAMPLE_CSV parses to exactly 2 accounts, 14 holdings total, with Account 900000001 fully eligible and Account 900000002 holding at least one CASH_SPECIAL_HANDLING, one ZERO_PRICE_NONZERO_VALUE_EXCEPTION, and one MISSING_LOOKUP_KEY row.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the per-account verdict spread and the synthetic-only guarantee</name>
  <files>demo-sample.test.cjs</files>
  <action>
Rewrite the assertions in demo-sample.test.cjs against the new sample, keeping the existing
require style (`./.test-dist/demo-sample.js`, `getHoldingEligibility`, `MANUAL_REVIEW_REQUIRED_CODES`).

- Replace `accounts.length === 1` with `assert.equal(ingestion.accounts.length, 2)` and add
  `assert.equal(ingestion.accounts.reduce((n, a) => n + a.holdings.length, 0), 14)`.
- Add a test: the FIRST account is fully clean — every holding is
  `getHoldingEligibility(h, { allowManualOverride: false }).eligible`, and the account has at
  least 5 holdings (this is the "what normal looks like" contrast the tour narrates).
- Rewrite the ok/review/block test to run against the SECOND account: assert it has at least one
  eligible row, at least TWO manual-review-gated rows (not eligible with override OFF, eligible
  with override ON, `requiresManualOverride` true) covering both CASH_SPECIAL_HANDLING and
  ZERO_PRICE_NONZERO_VALUE_EXCEPTION, and at least one row still blocked with override ON whose
  `blockedIssueCodes` includes `MISSING_LOOKUP_KEY`.
- Keep the SMPL-01 whole-run claim: assert that across BOTH accounts there is at least one ok,
  one review, and one block verdict in a single parse.
- Keep and extend the synthetic-identifier test: retain the real-ticker/real-name regex sweep of
  demo-sample.ts, keep the `900000001` assertion, and add a `900000002` assertion.
  </action>
  <verify>`node --test demo-sample.test.cjs` passes after `npx tsc -p tsconfig.test.json`.</verify>
  <done>demo-sample.test.cjs fails if the sample loses its second account, drops below 14 rows, lets Account 1 stop being clean, or loses either manual-review code or the blocked row.</done>
</task>

<task type="auto">
  <name>Task 3: Update the demo packet tests for the two-account sample</name>
  <files>demo-flow.test.cjs</files>
  <action>
demo-flow.test.cjs currently builds its packet from `ingestion.accounts[0]` only. Update
`buildDemoPacket(opts)` to take an account index (default 0) so both accounts can be exercised,
and update the existing tests:

- "demo packet (override off) carries only eligible rows...": run it for BOTH accounts in a loop.
  Keep the existing per-row eligibility mapping and the
  `packet.blockedCount === account.holdings.length - packet.holdings.length` assertion.
  Note that for Account 1 this means `blockedCount === 0` and the packet carries every row.
- "the manual-review override changes the packet contents": point it at the SECOND account
  (index 1) — that is where `$CASH$` now lives. Keep the assertions that `$CASH$` is absent with
  override OFF and present with override ON, and that the blank-ticker row never appears in
  either. Add: the zero-price row's ticker (DMOL) is absent with override OFF and present with
  override ON.
- Add a test asserting Account 1's override-OFF packet withholds nothing
  (`packet.blockedCount === 0` and `packet.rowCount === account.holdings.length`) — this is the
  SMPL-04 contrast the tour depends on.
- Leave the `demoRunStepOrder` tests and the "safety model is legible" test untouched.
  </action>
  <verify>`npm test` passes (tsc + all node --test suites).</verify>
  <done>demo-flow.test.cjs exercises both accounts, pins Account 1 as withholding nothing and Account 2 as withholding the gated rows, and `npm test` is green.</done>
</task>

</tasks>

<verification>
- `npm test` green (tsc + every node --test suite).
- `node -e` spot check: parsing DEMO_SAMPLE_CSV yields 2 accounts / 14 holdings.
- `grep -c ',' demo-sample.ts` style sanity: 14 data rows plus one header row.
- No real-world ticker, issuer, custodian, or person name appears in demo-sample.ts.
</verification>

<success_criteria>
- Two accounts, 14 rows, all synthetic.
- Account 1: every row eligible, nothing withheld.
- Account 2: eligible rows + two review-gated codes + one MISSING_LOOKUP_KEY block.
- Tests fail loudly if any of the above regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-destination-panel-sample/01-06-SUMMARY.md`.

Note for the next plans: main.ts's `onFillPacketReady` handler still keeps a single
`latestPacket`, so with two accounts the last account's packet wins. Plan 01-09 replaces that
with a per-account packet map. Do not fix it here.
</output>
