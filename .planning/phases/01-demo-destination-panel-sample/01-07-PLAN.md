---
phase: 01-demo-destination-panel-sample
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - main.ts
  - demo-destination-panel.test.cjs
autonomous: true
gap_closure: true
requirements: [DEMO-05]

must_haves:
  truths:
    - "Filling the destination panel twice leaves the same rows as filling it once — no duplicated tickers"
    - "Every fill in main.ts routes through one helper that resets the panel before writing rows"
    - "The panel still shows the filled rows in its results table after a fill completes"
  artifacts:
    - path: "main.ts"
      provides: "Single fill entry point that resets the panel before driving runDemoFill"
      contains: "runFillIntoPanel"
    - path: "demo-destination-panel.test.cjs"
      provides: "Regression pin that the fill path resets before filling and has exactly one runDemoFill call site"
      contains: "runFillIntoPanel"
  key_links:
    - from: "main.ts runFillIntoPanel"
      to: "demoPanel.reset()"
      via: "reset called before runDemoFill in the same function"
      pattern: "reset\\(\\)[\\s\\S]*runDemoFill"
    - from: "main.ts fillButton.onclick"
      to: "runFillIntoPanel"
      via: "button handler delegates instead of calling runDemoFill directly"
      pattern: "runFillIntoPanel\\("
---

<objective>
Make repeat fills idempotent: every fill resets the simulated destination panel before writing
rows, routed through a single helper in main.ts.

Purpose: closes UAT GAP 3 ("pressing add a holding keeps populating the same two random tickers
over and over"). `fillButton.onclick` calls `runDemoFill` directly, and `runDemoFill` clicks
"Add a Holding" per row, which appends to the panel's tbody. Nothing resets between presses, so
each press re-appends the same eligible rows.
Output: one `runFillIntoPanel` helper in main.ts (the only `runDemoFill` call site) plus a test
pinning the reset-then-fill ordering.
</objective>

<execution_context>
@C:/Users/Cyril/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Cyril/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-destination-panel-sample/01-UAT.md

@main.ts
@demo-destination-panel.ts
@paste-conductor.ts
@demo-destination-panel.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route every fill through a resetting runFillIntoPanel helper</name>
  <files>main.ts</files>
  <action>
In `renderLocalMvpShell`, add a single async helper next to `hideDestinationPanel`:

```
const runFillIntoPanel = async (packet: EmoneyFillPacket): Promise<void> => {
  if (!demoPanel) return;
  demoPanel.reset();            // idempotent fill: never append onto a prior fill's rows
  await runDemoFill(demoPanel.root, packet, {
    onRow: (row) => setStatus(status, `Filled row ${row.rowNumber}: ${row.ticker}`),
  });
};
```

Then rewrite `fillButton.onclick` to delegate to it — keep the existing guard
(`if (!latestPacket || !demoPanel) return;`), the disable/re-enable of `fillButton`, the
try/catch that reports errors via `setStatus(..., 'error')`, and the existing success line
(`${packet.rowCount} eligible rows filled. ${packet.blockedCount} blocked rows were withheld.`).
Only the middle — the direct `runDemoFill(...)` call — is replaced by `await runFillIntoPanel(packet)`.

Constraints:
- `runFillIntoPanel` must be the ONLY place in main.ts that calls `runDemoFill`. Later plans
  (the guided tour) will reuse it; do not add a second call site.
- Do NOT change `paste-conductor.ts`. `runDemoFill` stays label-driven, still never looks for or
  clicks Save, and the real bookmarklet's eMoney host guard stays untouched.
- Do NOT change `demo-destination-panel.ts`. `DemoDestinationPanel.reset()` already clears the
  tbody, clears the four inputs, clears the status line, and restores the "No rows yet." empty
  state — that is exactly the reset needed.
- Leave `hideDestinationPanel()` as-is; it already calls `demoPanel?.reset()`.
  </action>
  <verify>`npx tsc -p tsconfig.test.json` compiles; `grep -c "runDemoFill(" main.ts` shows exactly two occurrences (the import and the single call inside runFillIntoPanel).</verify>
  <done>Pressing the fill button twice runs reset -> fill both times, so the panel table holds `packet.rowCount` rows after either press.</done>
</task>

<task type="auto">
  <name>Task 2: Pin reset-before-fill as a regression test</name>
  <files>demo-destination-panel.test.cjs</files>
  <action>
Add tests to the existing suite (it already reads source text with `fs.readFileSync`; follow that
convention — there is no jsdom in this repo, and the real end-to-end DOM assertion is Phase 3's
headless check, VERI-01).

Read `main.ts` into a `mainSource` const, then assert:
1. `main.ts` contains exactly one `runDemoFill(` CALL — count occurrences of `runDemoFill(` and
   subtract the import line, or simply assert that the only call is inside `runFillIntoPanel` by
   slicing the helper body (from `const runFillIntoPanel` to the next top-level `const ` at the
   same indent) and asserting `runDemoFill(` appears in that slice and nowhere else in the file.
2. Inside that slice, `demoPanel.reset()` appears BEFORE `runDemoFill(`
   (`indexOf('reset()') < indexOf('runDemoFill(')`, both >= 0) — repeat fills must never append.
3. `fillButton.onclick` delegates: the sliced `fillButton.onclick` body contains
   `runFillIntoPanel(` and does NOT contain `runDemoFill(`.

Also add an assertion against `demo-destination-panel.ts` that `reset()` clears the table body
(`tbody.innerHTML = ''`) — that is the behavior the fix leans on.

Leave the existing tests in this file (trade dress, labeled fields, local-only save copy,
label-driven fill, untouched bookmarklet host guard) unchanged.
  </action>
  <verify>`node --test demo-destination-panel.test.cjs` passes; reverting the reset line in main.ts makes it fail.</verify>
  <done>The suite fails if a future edit adds a second `runDemoFill` call site or drops the reset before a fill.</done>
</task>

</tasks>

<verification>
- `npm test` green.
- `grep -n "runDemoFill" main.ts` shows the import plus one call, inside `runFillIntoPanel`.
- `grep -n "isApprovedEmoneyLocation" paste-conductor.ts` still present and unmodified.
</verification>

<success_criteria>
- One fill entry point in main.ts, resetting before every fill.
- Two consecutive fills leave the same row count as one fill.
- paste-conductor.ts and demo-destination-panel.ts unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-destination-panel-sample/01-07-SUMMARY.md`.
</output>
