---
phase: 01-demo-destination-panel-sample
plan: 09
type: execute
wave: 3
depends_on: ["01-08"]
files_modified:
  - main.ts
  - ledger-styles.ts
  - demo-flow.test.cjs
autonomous: true
gap_closure: true
requirements: [DEMO-01, DEMO-05, SMPL-04]

must_haves:
  truths:
    - "Clicking the demo button starts a click-through tour: each stage stops on an explainer card and waits for the visitor to press Next"
    - "No stage advances on a timer — the visitor controls the pace"
    - "Account 1 fills completely, its explainer lands while the filled panel is on screen, then Account 2 fills and its explainer names the withheld rows"
    - "Each account's fill starts from an empty panel, so the panel shows only that account's rows"
    - "Re-running the demo starts the tour over from stage 1 with a cleared panel"
  artifacts:
    - path: "main.ts"
      provides: "Tour card rendering, Next-button advance, per-account packet map, tour-driven fills"
      contains: "buildDemoTourSteps"
    - path: "ledger-styles.ts"
      provides: "Explainer card styling (.ledger-tour-card and friends)"
      contains: "ledger-tour"
    - path: "demo-flow.test.cjs"
      provides: "Regression pins that the demo handler is Next-driven, not timer-driven"
      contains: "advanceTour"
  key_links:
    - from: "main.ts sampleButton.onclick"
      to: "buildDemoTourSteps"
      via: "builds the tour from the parsed accounts instead of walking a timed loop"
      pattern: "buildDemoTourSteps\\("
    - from: "main.ts tour Next button"
      to: "advanceTour"
      via: "click handler advances one step"
      pattern: "nextButton\\.(onclick|addEventListener)"
    - from: "main.ts tour fill step"
      to: "runFillIntoPanel"
      via: "step.fillAccountNumber looks up that account's packet in the packet map"
      pattern: "runFillIntoPanel\\("
---

<objective>
Wire the guided tour to the page: replace the autoplay stage loop with an explainer card and a
Next button, and drive per-account fills from the tour.

Purpose: closes UAT GAP 1. Today `sampleButton.onclick` walks `demoRunStepOrder` on ~400ms
timers with status lines that name a stage but never explain it. After this plan the visitor
clicks Next through each stage, reads what just happened and what it proves, and watches
Account 1 fill clean before Account 2 stops on its gated rows.
Output: tour card UI + Next-driven state machine in main.ts, card styles in ledger-styles.ts,
tests pinning that nothing is timer-advanced.
</objective>

<execution_context>
@C:/Users/Cyril/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Cyril/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-destination-panel-sample/01-UAT.md

@main.ts
@ledger-styles.ts
@review-export-surface.ts
@demo-flow.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect per-account packets and render the tour card</name>
  <files>main.ts</files>
  <action>
Two changes inside `renderLocalMvpShell`:

(a) Per-account packets. `onFillPacketReady` fires once per account (it runs at the end of
`renderTransferPacket` inside `review-export-surface.ts`'s `ingestion.accounts.forEach`), so the
current single `latestPacket` variable is last-writer-wins and would only ever fill the last
account. Add `const packetsByAccount = new Map<string, EmoneyFillPacket>()` and, in the
`onFillPacketReady` handler, `packetsByAccount.set(packet.accountNumber, packet)` when `packet`
is non-null (and `delete` when null). Keep `latestPacket` for the existing `fillButton` path
(uploaded CSVs still use it) and keep the existing `withheldLine` text. Clear the map in
`hideDestinationPanel()`. Because the override checkbox re-render re-fires `onFillPacketReady`,
the map stays current by construction — do not add a second listener.

(b) Tour card DOM. Create a card element once, inserted between `reviewRoot` and
`destinationRoot` in the shell (so the visitor reads it with the review table above and the
destination panel below), hidden by default:
  - `<section class="ledger-tour-card" hidden>` with `role="region"` and `aria-live="polite"`
  - a step counter line ("Stage 2 of 5"), an `<h2>` for `step.title`, a `<p>` for `step.body`,
    a `<p class="ledger-tour-proof">` for `step.proof`, and a `<button class="ledger-button">`
    whose text is `step.nextLabel`.
Add a `renderTourStep(step, index, total)` helper that fills those elements and unhides the card.
Do not use `innerHTML` with interpolated step copy — set `textContent` (the copy is static, but
keep the pattern consistent with the rest of this file's user-facing strings).
  </action>
  <verify>`npx tsc -p tsconfig.test.json` compiles; `grep -n "packetsByAccount" main.ts` shows set, delete/clear, and read sites.</verify>
  <done>Parsing the two-account sample populates `packetsByAccount` with one entry per account that has eligible rows, and the tour card element exists and is hidden until a tour starts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the autoplay loop with the Next-driven tour</name>
  <files>main.ts</files>
  <action>
Rewrite the tail of `sampleButton.onclick`. Keep everything up to and including
`markSessionActive(ingestion)` as-is (hideDestinationPanel, `setWorkflowStep('load')`, skeleton,
`stageLoadStatus`, `runLocalMvp(...)` with its callbacks) — that part already works.

Then, instead of the `for (const step of demoRunStepOrder(...))` loop with `await delay(400)`:

1. Build the tour input from the parsed accounts, in `ingestion.accounts` order, including only
   accounts that produced a packet (`packetsByAccount.has(account.accountNumber)`):
   `{ accountNumber, totalRows: account.holdings.length, eligibleCount: packet.rowCount,
      withheldCount: packet.blockedCount }`.
2. `const steps = buildDemoTourSteps(tourAccounts);` and drive them with a small state machine:
   - `let tourIndex = -1;`
   - `const advanceTour = async () => { tourIndex += 1; ... }` — on each advance:
     a. if `tourIndex >= steps.length`, hide the card and stop;
     b. `setWorkflowStep(step.stage)` (this is now the single writer for the stepper on the demo
        path — nothing else may call `setWorkflowStep` during a tour);
     c. if `step.fillAccountNumber`: reveal `destinationRoot` (`hidden = false`), create the panel
        with `renderDemoDestinationPanel(destinationPanelHost)` if it does not exist yet, scroll
        it into view, disable the Next button while filling, then
        `await runFillIntoPanel(packetsByAccount.get(step.fillAccountNumber)!)` (from plan 01-07 —
        it resets the panel first, so each account starts from an empty table), then re-enable Next;
     d. `renderTourStep(step, tourIndex, steps.length)` — the explainer lands AFTER that account's
        fill completes, while the filled panel is still on screen;
     e. keep `setStatus(status, ...)` in sync with the step (short one-liner; the card carries the
        real explanation).
   - Wire the card's Next button to `void advanceTour()` (assign the handler once, at card
     creation, calling through a mutable `tourNext` reference so re-running the demo rebinds
     cleanly). Then call `void advanceTour()` once to show stage 1.
3. Re-running the demo must restart cleanly: `hideDestinationPanel()` at the top of the handler
   already clears the panel; also reset `tourIndex` to -1, clear `packetsByAccount`, and hide the
   card before the new run builds its steps.
4. Delete the `await delay(400)` pacing and, if `demoRunStepOrder` now has no callers, delete it
   from main.ts and remove its two tests from demo-flow.test.cjs — the equivalent invariants are
   pinned by the `buildDemoTourSteps` tests from plan 01-08 (tail step is a fill stage when
   packets exist; only load+review when none do). Do not leave an unused export behind.
5. Leave `fillButton` and its handler working for the uploaded-CSV path.
6. Do not touch `paste-conductor.ts`. Nothing in this plan may look for or click Save.
  </action>
  <verify>`npm test` passes; `grep -n "delay(400)" main.ts` returns nothing; `grep -n "setWorkflowStep" main.ts` shows the tour's single call site plus the non-demo paths (load/upload/clear).</verify>
  <done>Clicking "Run the full demo" shows stage 1 and waits; each Next advances exactly one stage; the two fill stages fill Account 1 then Account 2, each from an empty panel; re-clicking the demo button restarts at stage 1 with no leftover rows.</done>
</task>

<task type="auto">
  <name>Task 3: Style the explainer card and pin the tour as click-driven</name>
  <files>ledger-styles.ts, demo-flow.test.cjs</files>
  <action>
Styles (ledger-styles.ts): add rules inside the existing style-installer string for
`.ledger-tour-card` (prominent panel: card surface consistent with the existing `.ledger-panel`
tokens, generous padding, a clear step-counter eyebrow, readable measure ~60-70ch on the body
copy, `.ledger-tour-proof` visually distinct as the "what this proves" line, and the Next button
right-aligned and unmissable). Reuse the file's existing color/spacing tokens and class-naming
convention — do not introduce a new palette, and do not add any eMoney trade dress (no eMoney
colors, logo, or wordmark).

Tests (demo-flow.test.cjs): source-slice `main.ts` (same convention the existing
"safety model is legible" test uses) and assert:
1. The demo handler is Next-driven, not timer-driven: the sliced `sampleButton.onclick` body
   contains `advanceTour` and does NOT contain `delay(` (the pre-run `stageLoadStatus` call is
   outside the sliced tour section — slice from `buildDemoTourSteps` to the end of the handler if
   needed to keep this assertion precise).
2. `main.ts` contains no `demoRunStepOrder` reference if it was deleted, or exactly its original
   definition plus a call if it was kept — assert whichever end state Task 2 produced, so the file
   never carries a dead export.
3. The tour fills through the shared helper: the sliced `advanceTour` body contains
   `runFillIntoPanel(` and does NOT contain `runDemoFill(`.
4. Per-account fills read from the packet map: `main.ts` contains
   `packetsByAccount.get(step.fillAccountNumber` (or equivalent) and `packetsByAccount.set(`.
5. `ledger-styles.ts` defines `.ledger-tour-card` and contains no `emoney` string.
  </action>
  <verify>`npm test` passes; `npm run build:demo` succeeds.</verify>
  <done>The card is styled and legible, and the suite fails if the tour reverts to timer pacing, calls runDemoFill directly, or loses the per-account packet lookup.</done>
</task>

</tasks>

<verification>
- `npm test` green (tsc + every node --test suite).
- `npm run build:demo` succeeds.
- `grep -rn "emoney" ledger-styles.ts` returns nothing.
- `grep -n "runDemoFill(" main.ts` still shows exactly one call site (inside `runFillIntoPanel`).
- `grep -n "isApprovedEmoneyLocation\|isExpectedEmoneyHoldingsPage" paste-conductor.ts` unchanged.
</verification>

<success_criteria>
- The demo is a click-through tour: five stages, each waiting on a Next press.
- Account 1 fills all rows; Account 2 fills only its eligible rows and its card names the withheld ones.
- Each fill starts from an empty panel; re-running the demo restarts at stage 1.
- No timers pace the stages; no dead exports left behind.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-destination-panel-sample/01-09-SUMMARY.md`.

Then re-run the human browser verification (`01-05-PLAN.md`) against the tour — its steps predate
the guided tour, so verify the tour flow rather than the old autoplay flow.
</output>
