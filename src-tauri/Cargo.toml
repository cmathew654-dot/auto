[package]
name = "holdings-emoney-assistant"
version = "0.1.3"
description = "Holdings CSV review and guided browser entry"
authors = ["Cyril"]
edition = "2021"

[lib]
name = "holdings_emoney_assistant_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
