{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local Holdings Entry Assistant window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
